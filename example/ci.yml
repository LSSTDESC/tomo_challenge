metrics: [SNR_ww, SNR_gg, SNR_3x2]
bands: riz
training_file: data/mini_training.hdf5
validation_file: data/mini_validation.hdf5
output_file: example/ci_output.txt

run:
  RandomForest:
    # Baseline run
    run_3:
      bins: 3

    # Also classifying on colours and errors
    run_3_ce:
      bins: 3
      colors: True
      errors: True

  # i-band uniform split
  IBandOnly:
    run_2:
      bins: 2
        
