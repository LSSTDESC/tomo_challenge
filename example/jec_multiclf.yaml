metrics:  [SNR_3x2, FOM_3x2]
bands: griz
training_file: data/training.hdf5
validation_file: data/validation.hdf5
output_file: example/jec_MultiClf-GB+RFNoDepthLimit-StdScaler-8bins-50estim-griz-testDump.txt

run:
  # This is a class name which will be looked up
  myCombinedClassifiers:
    run_3:
      savefile: /Users/campagne/Travail/Software/tomo_challenge-orig/data/jec__MultiClf-GB+RFNoDepthLimit-StdScaler-8bins-50estim-griz-testDump.joblib
      n_estimators: 50
      # This setting is sent to the classifier
      bins: 8
      # These special settings decide whether the
      # color and error colums are passed to the classifier
      # as well as the magnitudes
      colors: True
      errors: False
